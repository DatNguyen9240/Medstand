# ── Node.js portable (không cần track) ──
HoangDang/nodejs/
n8n-system/node-v*

# ── n8n runtime data (credentials, cache, logs) ──
n8n-system/n8n_data/

# ── n8n-system runtime (DB, credentials, encryption keys, logs) ──
n8n-system/n8n/
n8n-system/n8n_data/
n8n-system/*.db
n8n-system/*.sqlite
n8n-system/logs/

# ── Cloudflared binary ──
n8n-system/cloudflared.exe

# ── File test tạm ──
n8n-system/test_n8n.ps1

# ── Node modules ──
node_modules/

# ── Scratch / temp files (tạo bởi AI tools) ──
scratch*
*.scratch.*

# ?? n8n-system binaries (node, cloudflared) ??
n8n-system/bin/
